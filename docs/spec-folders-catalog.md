# 仕様: フォルダタブ（品目名・店名カタログ）

最終更新: 2026-08-28

関連: [project-overview.md](./project-overview.md) / [spec-shopping-memo.md](./spec-shopping-memo.md)

## 1. 役割

フォルダタブは **記録データのハブ** である。

| 概念 | テーブル | 意味 |
|------|----------|------|
| 品目名（フォルダ） | `price_folders` | 比較したい商品集合の棚。記録は必ずここに属する |
| 店名（店舗） | `price_stores` | 店舗名の正規カタログ。記録の `store_name` と整合させる |
| 記録 | `price_records` | 購入日・店・値段・数量などの厳密データ |

買い物メモの一覧は **別管理**（`price_memo_items`）。フォルダタブで棚を作っても、メモに載せない限り店頭一覧には出ない。

## 2. UI 構成

### 2.1 タブ内切替

画面上部で **品目名** / **店名** を切り替える。レイアウトは共通:

- 先頭の水色カード … 追加（＋）
- グリッド … カード一覧（品目は琥珀色、店名は緑色）
- 検索欄 … 名前の部分一致
- 並び … **追加順（新しい順）** または **名前順**

### 2.2 品目名カード

- 表示名は末尾の `()` / `（）` 内の **読み** を一覧では非表示（ソート・検索では利用）
- 例: 保存名 `牛乳（ぎゅうにゅう）` → 表示 `牛乳`、名前順は `ぎゅうにゅう` 基準
- 鉛筆（名前変更）は品目名の直右。小さく薄い `quiet` スタイル
- 右端: 記録件数・展開矢印・**記録追加（＋）**
- 展開でフォルダ内記録一覧。記録はドラッグで並べ替え・ゴミ箱で削除
- **値段推移（PC）** … グラフボタンで画面左半分にカタログ全体、右半分に推移グラフ（全幅レイアウト）
- 記録の編集は **モーダル**（追加と同様）

### 2.3 店名カード

- 品目名と同様のヘッダー（件数・展開・記録追加）
- 展開で当店舗の記録一覧（全フォルダ横断）
- 記録追加（＋）… モーダルで **品目（フォルダ）を選択** し、店名は事前入力
- 店名変更時は、同店名の既存記録の `store_name` も一括更新

### 2.4 ドラッグ・ゴミ箱

| 対象 | 並べ替え | ゴミ箱 |
|------|----------|--------|
| 品目名カード | なし | 削除（記録あり → 確認、0件 → 即削除） |
| 店名カード | なし | 一覧から削除（記録あり → 確認、0件 → 即削除。記録自体は残る） |
| フォルダ内記録 | あり（DB `sort_order` 永続） | 即削除 |

## 3. 店舗名の入力（アプリ全体）

記録保存時の店舗は **自由入力ではなくカタログ指定** を基本とする。

- UI: `StoreField`（検索＋一覧から選択、未登録は「〇〇を店舗に登録」）
- 保存時: `ensureStore` で `price_stores` に登録し、正規名を `price_records.store_name` に書く
- 既存記録からのバックフィルで `price_stores` を初期化済み

対象画面: フォルダタブの記録モーダル、買い物メモの保存フォーム など。

## 4. 新規品目の追加フロー

1. 先頭の＋カードを押す
2. `新しいフォルダ` 等の仮名で作成（**追加順では末尾**）
3. 即座に名前編集状態（名前順表示時は追加カードの隣に固定し、保存後にソート位置へ）

## 5. 実装パス

| 領域 | パス |
|------|------|
| フォルダタブ UI | `frontend/src/features/folders/components/FoldersPage.tsx` |
| フォルダ API / hook | `frontend/src/features/folders/` |
| 店舗 API | `frontend/src/features/stores/` |
| 店舗入力 | `frontend/src/components/StoreField.tsx` |
| 記録フォーム | `frontend/src/features/records/components/RecordForm.tsx` |
| 読み付き名前 | `frontend/src/features/folders/utils/folderName.ts` |
| ドラッグ | `frontend/src/components/trash/` |

## 6. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-28 | 品目名／店名切替、店舗カタログ、検索・並び、記録モーダル編集、削除確認、カード高さ統一 |
| 2026-08-28 | フォルダ並べ替えドラッグ廃止（ゴミ箱のみ）。読み付きフォルダ名 |
