{
  "name": "price-memo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"FRONT,BACK\" -c \"cyan,magenta\" \"npm run dev --prefix frontend\" \"cd backend && uv run uvicorn app.main:app --reload --port 8001\"",
    "db:push": "npx supabase db push --linked",
    "db:list": "npx supabase migration list"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "supabase": "^2.115.0"
  }
}
